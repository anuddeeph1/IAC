# yaml-language-server: $schema=https://raw.githubusercontent.com/kyverno/chainsaw/main/.schemas/json/test-chainsaw-v1alpha1.json
apiVersion: chainsaw.kyverno.io/v1alpha1
kind: Test
metadata:
  name: good-test-01
spec:
  steps:
    - name: kyverno-json
      try:
      - script:
          content: |
            set -e
            kyverno-json scan --payload ./good-test/good-payload-01.json --policy ../check-ec2-vpc-tags.yaml --output json
          check:
            ($error): ~
            (json_parse($stdout)):
            - results:
              - policy:
                  apiVersion: json.kyverno.io/v1alpha1
                  kind: ValidatingPolicy
                  metadata:
                    name: check-ec2-vpc-tags
                rules:
                - rule:
                    name: check-ec2-vpc-tags
                  error: ~
                  violations: ~
---
apiVersion: chainsaw.kyverno.io/v1alpha1
kind: Test
metadata:
  name: bad-test-01
spec:
  steps:
    - name: kyverno-json
      try:
      - script:
          content: |
            set -e
            kyverno-json scan --payload ./bad-test/bad-payload-01.json --policy ../check-ec2-vpc-tags.yaml --output json
          check:
            ($error): ~
            (json_parse($stdout)):
            - results:
              - policy:
                  apiVersion: json.kyverno.io/v1alpha1
                  kind: ValidatingPolicy
                  metadata:
                    name: check-ec2-vpc-tags
                rules:
                - rule:
                    name: check-ec2-vpc-tags
                  error: ~
                  violations:
                  - (contains(message, 'VPCs must be tagged with the required keys ')): true
                    errors:
                    - type: FieldValueInvalid
                      value: false
                      detail: "Expected value: true"
                    - type: FieldValueInvalid
                      value: false
                      detail: "Expected value: true"
---
apiVersion: chainsaw.kyverno.io/v1alpha1
kind: Test
metadata:
  name: bad-test-02
spec:
  steps:
    - name: kyverno-json
      try:
      - script:
          content: |
            set -e
            kyverno-json scan --payload ./bad-test/bad-payload-02.json --policy ../check-ec2-vpc-tags.yaml --output json
          check:
            ($error): ~
            (json_parse($stdout)):
            - results:
              - policy:
                  apiVersion: json.kyverno.io/v1alpha1
                  kind: ValidatingPolicy
                  metadata:
                    name: check-ec2-vpc-tags
                rules:
                - rule:
                    name: check-ec2-vpc-tags
                  error: ~
                  violations:
                  - (contains(message, 'VPCs must be tagged with the required keys ')): true
                    errors:
                    - type: FieldValueInvalid
                      value: false
                      detail: "Expected value: true"